/*
parseInt is a function that takes in a string contianing numbers and converts it to an integer.
parseFloat will convert to a float.
*/

const myString = '12345';

const myInteger = parseInt(myString)


console.log(typeof(myInteger))
